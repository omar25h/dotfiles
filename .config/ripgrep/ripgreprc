# Add types
--type-add
gomod:go.mod

--type-add
gosum:go.sum

--type-add
env:*.env

--type-add
properties:*.properties

--type-add
terraform:*.tf

--type-add
jenkins:Jenkinsfile*

--type-add
jenkins:Buildfile

# Using glob patterns to include/exclude files or folders
--glob=!.git/*

# Always include hidden files, unless --no-hidden is passed
--hidden

--smart-case

